This script scrapes the website of a popular petstore, and stores the data in a MongoDB database.
New records are inserted into the MongoDB database one per week. This script runs on an AWS EC2 instance.
